<% content_for :title, "Products" %>

<h1>Products</h1>

<div id="products">
  <% if @products.any? %>
    <% @products.each do |product| %>
      <%= render product %>
    <% end %>
  <% else %>
    <p>No products available.</p>
  <% end %>
</div>
