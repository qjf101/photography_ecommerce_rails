<h1>Thank you for your order!</h1>

<p>Order confirmation: <%= @order.confirmation_token %></p>

<% @order.line_items.each do |line_item| %>
  <p>
    <%= line_item.product.name %> &times; <%= line_item.quantity %>
    &mdash; <%= number_to_currency((line_item.quantity * line_item.unit_price_cents) / 100.0) %>
  </p>
<% end %>

<p><strong>Total: <%= number_to_currency(@order.total_cents / 100.0) %></strong></p>

<% if @order.shipping_name.present? %>
  <h2>Shipping to</h2>
  <p>
    <%= @order.shipping_name %><br>
    <%= @order.shipping_address_line1 %><br>
    <% if @order.shipping_address_line2.present? %>
      <%= @order.shipping_address_line2 %><br>
    <% end %>
    <%= @order.shipping_city %>, <%= @order.shipping_state %> <%= @order.shipping_postal_code %><br>
    <%= @order.shipping_country %>
  </p>
<% end %>

<p>
  You can check your order status anytime at
  <%= link_to order_url(@order), order_url(@order) %>.
</p>
